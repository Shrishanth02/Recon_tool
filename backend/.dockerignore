# Python bytecode / caches
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Local SQLite database (Postgres is used in the container)
*.db
reconx.db

# Local env / secrets — never bake into the image
.env
.env.*
!.env.example

# Dev-only requirements
requirements-dev.txt

# Docker / VCS / editor cruft
Dockerfile
.dockerignore
.git/
.gitignore
.idea/
.vscode/
*.log
