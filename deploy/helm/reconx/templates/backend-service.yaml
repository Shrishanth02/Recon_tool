apiVersion: v1
kind: Service
metadata:
  name: {{ include "reconx.fullname" . }}-backend
  labels:
    {{- include "reconx.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  type: {{ .Values.backend.service.type | default .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "reconx.componentSelectorLabels" (dict "root" . "component" "backend") | nindent 4 }}
